generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "file:./dev.db"
}

model Page {
  id        String   @id
  content   String
  editToken String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}